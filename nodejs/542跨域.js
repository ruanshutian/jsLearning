/**
 *
 * 跨域问题：
 * 1、cors
 * 2、jsonp（只支持get请求）；
 *
 */

/**
 *
 * cors使用
 *
 * npm install cors
 * const cors = require('cors');
 * app.use(cors());
 *
 * 1、服务端配置。客户端无需任何额外的配置
 * 2、cors在浏览器中有兼容性，只有支持XMLHttpRequest Level2的浏览器，才能正常访问开启cors的服务端接口
 * （IE10+，chrome4+、FireFox3。5+）
 *
 */






